const actionFunctionreducer = (state, action) => {
    switch (action.type) {
      case "delete":
   
        return state;
      case "edit":
        return state;
      case "view":


        return state;
      default:
        return state;
    }
  };

export default actionFunctionreducer;
